# fullsend shim workflow
# Routes events to agent workflows in .fullsend via repository_dispatch.
#
# Credentials: uses ONLY secrets.FULLSEND_DISPATCH_TOKEN (PAT) to send
# repository_dispatch events to owner/.fullsend. Agent workflows in .fullsend
# listen for these events (fullsend-triage, fullsend-code, fullsend-review).
#
# Security: pull_request_target runs the BASE branch version of this workflow,
# preventing PRs from modifying it to exfiltrate the dispatch token.
# This shim never checks out PR code, so it is not vulnerable to "pwn request"
# attacks (see: Trivy CVE-2026-33634, hackerbot-claw campaign).
#
# The event payload is passed via an environment variable (not inline shell)
# to prevent script injection from attacker-controlled fields like issue
# titles, comment bodies, and PR descriptions.
name: fullsend

permissions:
  contents: read

on:
  issues:
    types: [labeled]
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize, ready_for_review]

jobs:
  dispatch-triage:
    runs-on: ubuntu-latest
    concurrency:
      group: triage-${{ github.event.issue.number || github.event.pull_request.number }}
      cancel-in-progress: true
    if: >-
      github.event_name == 'issue_comment' && (
        github.event.comment.body == '/triage' ||
        startsWith(github.event.comment.body, '/triage ') ||
        (
          (github.event.comment.author_association != 'NONE' ||
            github.event.comment.user.login == github.event.issue.user.login) &&
          !endsWith(github.event.comment.user.login, '[bot]') &&
          contains(toJSON(github.event.issue.labels.*.name), 'needs-info') &&
          !contains(toJSON(github.event.issue.labels.*.name), 'type/feature')
        )
      )
    steps:
      - name: Dispatch triage stage
        env:
          GH_TOKEN: ${{ secrets.FULLSEND_DISPATCH_TOKEN }}
          EVENT_PAYLOAD: ${{ toJSON(github.event) }}
          EVENT_TYPE: ${{ github.event_name }}
          SOURCE_REPO: ${{ github.repository }}
          DISPATCH_REPO: ${{ github.repository_owner }}/.fullsend
        run: |
          body="$(jq -n \
            --arg et "fullsend-triage" \
            --arg oet "$EVENT_TYPE" \
            --arg sr "$SOURCE_REPO" \
            --arg ep "$EVENT_PAYLOAD" \
            '{event_type: $et, client_payload: {event_type: $oet, source_repo: $sr, event_payload: $ep}}')"
          printf '%s' "$body" | gh api --method POST "repos/$DISPATCH_REPO/dispatches" --input -

  dispatch-code:
    runs-on: ubuntu-latest
    if: >-
      (github.event_name == 'issues' && github.event.action == 'labeled'
        && github.event.label.name == 'ready-to-code') ||
      (github.event_name == 'issue_comment' && (
        github.event.comment.body == '/code' ||
        startsWith(github.event.comment.body, '/code ')
      ))
    steps:
      - name: Dispatch code stage
        env:
          GH_TOKEN: ${{ secrets.FULLSEND_DISPATCH_TOKEN }}
          EVENT_PAYLOAD: ${{ toJSON(github.event) }}
          EVENT_TYPE: ${{ github.event_name }}
          SOURCE_REPO: ${{ github.repository }}
          DISPATCH_REPO: ${{ github.repository_owner }}/.fullsend
        run: |
          body="$(jq -n \
            --arg et "fullsend-code" \
            --arg oet "$EVENT_TYPE" \
            --arg sr "$SOURCE_REPO" \
            --arg ep "$EVENT_PAYLOAD" \
            '{event_type: $et, client_payload: {event_type: $oet, source_repo: $sr, event_payload: $ep}}')"
          printf '%s' "$body" | gh api --method POST "repos/$DISPATCH_REPO/dispatches" --input -

  dispatch-review:
    runs-on: ubuntu-latest
    if: >-
      (github.event_name == 'issues' && github.event.action == 'labeled'
        && github.event.label.name == 'ready-for-review') ||
      (github.event_name == 'issue_comment' && (
        github.event.comment.body == '/review' ||
        startsWith(github.event.comment.body, '/review ')
      )) ||
      github.event_name == 'pull_request_target'
    steps:
      - name: Dispatch review stage
        env:
          GH_TOKEN: ${{ secrets.FULLSEND_DISPATCH_TOKEN }}
          EVENT_PAYLOAD: ${{ toJSON(github.event) }}
          EVENT_TYPE: ${{ github.event_name }}
          SOURCE_REPO: ${{ github.repository }}
          DISPATCH_REPO: ${{ github.repository_owner }}/.fullsend
        run: |
          body="$(jq -n \
            --arg et "fullsend-review" \
            --arg oet "$EVENT_TYPE" \
            --arg sr "$SOURCE_REPO" \
            --arg ep "$EVENT_PAYLOAD" \
            '{event_type: $et, client_payload: {event_type: $oet, source_repo: $sr, event_payload: $ep}}')"
          printf '%s' "$body" | gh api --method POST "repos/$DISPATCH_REPO/dispatches" --input -
